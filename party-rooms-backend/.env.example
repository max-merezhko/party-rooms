SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional but recommended for this API: server-side reads/writes without per-table RLS tuning.
# Never expose this key in the frontend or GitHub Pages build.
SUPABASE_SERVICE_ROLE_KEY=

YOUTUBE_API_KEY=
PORT=3000
NODE_ENV=development
FRONTEND_URL_LOCAL=http://localhost:5173
FRONTEND_URL_PROD=